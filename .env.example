# Example configuration for agent-proxy

# Port to expose the HTTP service on
PORT=8080

# Shared secret for your ChatGPT agent. The agent must include
# this value in the `X-Bridge-Token` header for the proxy to accept the
# request. Leave empty to disable token validation.
BRIDGE_TOKEN=my-secret-token

# Expected value for the `Signature-Agent` header. The agent sends
# this header to assert its identity. Remove surrounding quotes if you
# copy/paste from an example. Default is https://chatgpt.com.
EXPECTED_AGENT=https://chatgpt.com

# Timeout (in milliseconds) for outbound fetch requests to remote
# services.
REQUEST_TIMEOUT_MS=25000

# OAuth 2.0 (optional)
# If set, incoming requests must include an Authorization: Bearer <token>
# header. The token will be validated via RFC 7662 token introspection.
#
# OAUTH_INTROSPECTION_URL=https://auth.example.com/oauth2/introspect
# OAUTH_CLIENT_ID=bridge-client
# OAUTH_CLIENT_SECRET=bridge-secret
# OAUTH_REQUIRED_SCOPES=agent:proxy
# OAUTH_REQUIRED_AUDIENCE=chatgpt-proxy
# OAUTH_CACHE_TTL_MS=60000

# Debug logging (optional): set to 1/true/yes for verbose logs
# DEBUG=1